# Downloaded client bundles, HARs, scratch captures — reproducible, not source
artifacts/
*.har

# Never commit anything from an authenticated session
**/session/
*.session.json
*cookies*
*token*
!docs/**

# Editors / OS
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# Per-machine Claude Code settings; .claude/launch.json (shared dev config) IS tracked.
.claude/settings.local.json
